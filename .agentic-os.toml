# umbra is deliberately unguarded: it is the umbra framework itself,
# not a consumer of it. It carries no .ward catalog config and does not
# route dev verbs through a guard (dev verbs are plain `make`). Disable the
# catalog hooks that require a catalog config. See the umbra
# unguarding decision (structural validation of the consumer flow).

[catalog-block-present]
enabled = false

[catalog-trifecta]
enabled = false

# .pre-commit-config.yaml carries the `# BEGIN/END managed by ...` markers the
# agentic-os hook installer needs mid-file, so it cannot meet the one-comment
# YAML rule. agentic-os excludes its own copy for the same reason.
[code-comments]
excludes = [
  ".pre-commit-config.yaml",
]
